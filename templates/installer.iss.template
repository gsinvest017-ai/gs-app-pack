; Generated by gs-app-pack -- do not edit directly.
; Source template: gs-app-pack/templates/installer.iss.template

#define AppName    "__APP_NAME__"
#define AppVersion "__APP_VERSION__"
#define AppExeName "__APP_EXE__.exe"

[Setup]
AppId={{__APP_ID__}
AppName={#AppName}
AppVersion={#AppVersion}
AppPublisher=__APP_PUBLISHER__
AppPublisherURL=__APP_URL__
VersionInfoVersion={#AppVersion}

PrivilegesRequired=lowest
DefaultDirName={localappdata}\{#AppName}
DefaultGroupName={#AppName}
DisableProgramGroupPage=yes

OutputDir=dist
OutputBaseFilename=__APP_EXE__-setup
Compression=lzma2/ultra64
SolidCompression=yes
WizardStyle=modern
ShowLanguageDialog=no

SetupIconFile=static\gs-icon.ico
UninstallDisplayIcon={app}\{#AppExeName}

[Languages]
Name: "english"; MessagesFile: "compiler:Default.isl"

[Tasks]
Name: "desktopicon"; Description: "Create a &desktop shortcut"; GroupDescription: "Additional icons:"; Flags: checkedonce

[InstallDelete]
; Clear the bundled payload before writing the new one. Inno overwrites files it
; is installing but never removes ones the new build dropped, so a dependency
; taken out of the build lingers on every upgraded machine: removing PaddleOCR
; from autogo cut 1.2 GB from the installer, and upgraded installs still carried
; all of it. Worse than the disk cost, _internal is on sys.path at runtime, so a
; stale package can still be imported and picked over the one that replaced it.
; User data is not here — it lives in %APPDATA% — so this is safe to wipe.
Type: filesandordirs; Name: "{app}\_internal"
__EXTRA_DELETE__

[Files]
Source: "dist\__APP_EXE__\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

[Icons]
Name: "{userprograms}\{#AppName}";           Filename: "{app}\{#AppExeName}"
Name: "{userprograms}\Uninstall {#AppName}"; Filename: "{uninstallexe}"
Name: "{userdesktop}\{#AppName}";            Filename: "{app}\{#AppExeName}"; Tasks: desktopicon

[Run]
Filename: "{app}\{#AppExeName}"; Description: "Launch {#AppName} now"; Flags: nowait postinstall skipifsilent

[Code]

function InitializeSetup(): Boolean;
var
  ResultCode: Integer;
begin
  Exec('taskkill.exe', '/f /im __APP_EXE__.exe', '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
  Sleep(500);
  Result := True;
end;

__GH_BLOCK__
